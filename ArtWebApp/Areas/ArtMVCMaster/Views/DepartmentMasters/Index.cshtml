@model IEnumerable<ArtWebApp.DataModels.DepartmentMaster>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/ArtMVC/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DepartmentName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DepartmentName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Deapartment_PK }) |
            @Html.ActionLink("Details", "Details", new { id=item.Deapartment_PK }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Deapartment_PK })
        </td>
    </tr>
}

</table>
