@model IEnumerable<ArtWebApp.DataModels.TemplateConstruction>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/ArtMVC/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Construct)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Template_Master.TemplateCode)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Construct)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Template_Master.TemplateCode)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TemplateCon_Pk }) |
            @Html.ActionLink("Details", "Details", new { id=item.TemplateCon_Pk }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TemplateCon_Pk })
        </td>
    </tr>
}

</table>
