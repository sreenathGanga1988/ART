@model IEnumerable<ArtWebApp.DataModels.BuyerDestinationMaster>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/ArtMVC/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BuyerDestination)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BuyerMaster.BuyerName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BuyerDestination)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BuyerMaster.BuyerName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BuyerDestination_PK }) |
            @Html.ActionLink("Details", "Details", new { id=item.BuyerDestination_PK }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BuyerDestination_PK })
        </td>
    </tr>
}

</table>
